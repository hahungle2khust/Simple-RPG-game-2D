public enum Dir
{
    Stand = 0,
    Up = 1,
    Down = 2,
    Left = 3,
    Right = 4
}

public enum Team
{
    Ally,
    Enemy,
    Both
}

public enum Status
{
    Poisoned,
    Paralyzed,
    Burned,
    Count
}

public enum HardMode
{
    Easy = 70,
    Normal = 100,
    Hard = 130,
}

public enum SkillType
{
    Normal,
    Special
}